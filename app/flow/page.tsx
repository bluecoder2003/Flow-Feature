import FlowApp from './FlowApp';

export const metadata = {
  title: 'Flow — AI Routine Assistant',
  description: 'AI-powered routine and commute assistant',
};

export default function FlowPage() {
  return <FlowApp />;
}
